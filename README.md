A simple command-line to-do list application. Users should be able to add, delete, mark as done, and list tasks,

Implemented the ability to add, view, and delete tasks.
Stores the tasks in a file (either CSV or JSON format).
Mark tasks as completed.
Implemented basic error handling (e.g., trying to delete a task that doesn’t exist)
